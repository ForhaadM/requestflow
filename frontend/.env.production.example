# Copy to frontend/.env.production (or set in your build environment) once
# you know your deployed backend's public URL (EC2 public DNS, or better,
# a domain/ALB in front of it).
VITE_API_URL=https://your-backend-domain-or-ec2-public-dns:8000
